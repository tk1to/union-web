<header>
	<h1 class="heading ta_center">お問い合わせ一覧</h1>
</header>

<div class="list__wrap">
	<ul>
	  <% @contacts.each do |contact| %>
	    <li class="list">
	    	<%= link_to contact.send_user.name, contact.send_user , class: "list__name_contact"%>
	    	<%= link_to contact.content, [@circle, contact] , class: "list__subhead_contact"%>
	    </li>
	  <% end %>
	</ul>
	<%= link_to "サークルへ戻る", @circle ,class: "buttun buttun_size_medium buttun_color_gray"%>
</div>