<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.content, [@circle, contact] %> | <%= link_to contact.send_user.name, contact.send_user %></li>
  <% end %>
</ul>
<p><%= link_to "サークルへ戻る", @circle %></p>