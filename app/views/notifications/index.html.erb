
<header>
  <h1 class="formHeading ta_center">通知</h1>
</header>

<ul>
  <% @notifications.each do |notification| %>
    <li class="notice">
      <% if notification.followed? %>
        <% user = notification.user %>
        <%= link_to user do %>
          <div class="notice__description"><%= user.name %>さんがあなたをフォローしました。</div>
          <div class="notice__content noticeFollow">
            <div class="noticeFollow__image" style="background-image: url(<%= user.header_picture %>)"></div>
            <div class="noticeFollow__heading"><%= user.name %></div>
            <div class="noticeFollow__subhead">
              <%= user.college %>/<%= user.department %>/<%= user.grade %>年
            </div>
          </div>
        <% end %>
      <% end %>
    </li>

    <li class="notice">
      <div class="notice__description"><%= user.name %>さんが新しいブログを作成しました。</div>
      <div class="notice__content noticeBlog">
        <div class="noticeBlog__image" style="background-image: url(<%= user.header_picture %>)"></div>
        <div class="noticeBlog__heading">ブログタイトル</div>
        <div class="noticeBlog__subhead">初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。
        </div>
      </div>
    </li>

    <li class="notice">
      <div class="notice__description">さーくるさんが新しいブログを作成しました。</div>
      <div class="notice__content noticeBlog">
        <div class="noticeBlog__image" ></div>
        <div class="noticeBlog__heading">ブログタイトル</div>
        <div class="noticeBlog__subhead">初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。初めまして、ブログの中身が入ります。
        </div>
      </div>
    </li>
  <% end %>
</ul>
