<%= form_for [@circle, current_user.favorites.build], remote: true do |f| %>
  <%= hidden_field_tag :circle_id, @circle.id %>
  <%= f.submit "気になる" ,class: "favorites"%>
  <%= image_tag 'pin_fff.svg' , class: "favorites_icon"%>
<% end %>